using namespace std;

template <int T>

class Fraction {
private:
	int numerator;
	int denominator;
public:
	

	Fraction()
	{
	
	}

	Fraction(int numerator, int denominator) 
	{
		if (den == 0) 
		{
			throw invalid_argument("Denominator cannot be 0");
		}
		denominator = den;
	}

	const simplify() 
	{
	
	}

	const toDoubble()
	{
	
	}
};